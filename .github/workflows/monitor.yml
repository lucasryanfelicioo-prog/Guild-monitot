name: Monitor Guild NTO

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: monitor-guild-nto
  cancel-in-progress: false

jobs:
  check-guild:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar o repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install requests beautifulsoup4

      - name: Rodar checagem da guild (level, entrada/saida, online)
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        run: python monitor_guild.py

      - name: Rodar checagem de mortes + war (AKATSUKI x NIRVANA)
        env:
          DEATH_WEBHOOK_URL: ${{ secrets.DEATH_WEBHOOK_URL }}
          WAR_WEBHOOK_URL: ${{ secrets.WAR_WEBHOOK_URL }}
        run: python monitor_deaths_pretty.py

      - name: Salvar estado atualizado no repositorio
        run: |
          git config user.name "guild-monitor-bot"
          git config user.email "actions@github.com"
          git add guild_state.json death_state.json war_state.json
          git diff --quiet --cached || git commit -m "Atualiza estado da guild [skip ci]"
          git pull --rebase origin main
          git push
